# Garment Worker Productivity Prediction

This project predicts garment worker productivity using machine learning
and deploys the model using Flask.

## Steps to Run
1. Install dependencies
   pip install -r requirements.txt

2. Go to Flask folder
   python app.py

3. Open browser
   http://127.0.0.1:5000/
